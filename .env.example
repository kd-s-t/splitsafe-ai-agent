# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
# (All NEXT_PUBLIC_* variables are exposed to browser)
# Copy this file to .env.local and update values as needed

# ICP / DFX Configuration
NEXT_PUBLIC_DFX_HOST=http://localhost:4943
NEXT_PUBLIC_CANISTER_ID_SPLIT_DAPP=your-canister-id-here

# Application URLs
NEXT_PUBLIC_APP_URL=https://thesplitsafe.com
NEXT_PUBLIC_BLOCKSTREAM_URL=https://blockstream.info
NEXT_PUBLIC_ICP_DASHBOARD_URL=https://dashboard.internetcomputer.org

# Backend API URL (optional - defaults to Vercel backend)
# Uncomment to use local backend during development
# NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Admin Configuration
NEXT_PUBLIC_ADMIN_PRINCIPAL=your-admin-principal-here

# Pusher (Real-time notifications)
NEXT_PUBLIC_PUSHER_APP_ID=your-pusher-app-id
NEXT_PUBLIC_PUSHER_KEY=your-pusher-key
NEXT_PUBLIC_PUSHER_CLUSTER=your-pusher-cluster

# Environment
NODE_ENV=development
