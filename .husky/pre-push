echo "🔍 Running pre-push checks..."

# Run build (type-check skipped due to pre-existing errors)
echo "🏗️  Running build..."
npm run build
if [ $? -ne 0 ]; then
  echo "❌ Build failed. Please fix the issues before pushing."
  exit 1
fi

echo "✅ All pre-push checks passed!"